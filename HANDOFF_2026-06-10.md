# 주식관련AI — 인수인계 (2026-06-10)

> 이 문서가 **현재 최신**이다. 이전 `HANDOFF_2026-06-09_for_codex.md` 를 대체한다.
> 운영 규칙·세부 불변식은 `CLAUDE.md`(저장소 루트)에 더 자세히 있으니 함께 읽을 것.

- 로컬: `C:\Users\jin\Desktop\주식관련AI`
- GitHub: https://github.com/jinnn99/sp-profit-plan  (기본 브랜치 `master`)
- 운영 배포: https://sweetproduct.pages.dev  (Cloudflare Pages)
- 현재 HEAD: `f077487`

---

## 0. 절대 규칙 (변경 금지)

1. **$0 비용 최우선.** 유료 API/결제/free-tier 초과/always-on/크론·트래픽·스토리지 증가 등 비용 위험이 있는 변경은 **구현·권장 전에 사용자에게 경고하고 명시 승인**을 받는다. 불확실하면 멈추고 질문.
2. **리포트는 생성기 단일 소스.** 모든 디자인/마크업/CSS/JS 는 `stock_ai/report/recommend_report.py` 가 찍어낸다. 생성물 `S&P_수익&플랜.html` 을 직접 손대지 말 것(임시 빠른경로 제외).
3. **제거된 UI 복구 금지**: 면책/주의 배너, 푸터, 히어로 메타줄, 표 하단 안내문, 포트폴리오 비중 패널, 표 비중열. + 모바일 eyebrow 숨김 유지.
4. **D1 binding 이름은 `DB` 고정.** 서비스워커는 `/api/` 캐시 금지, `_headers` 의 `/api/* no-store` 유지.
5. **순위 파리티 불변식**: 브라우저 슬라이더가 전 종목을 클라이언트에서 재랭킹하는데, 그 결과가 서버와 **수학적으로 동일**해야 한다. `_RANK_JS`(recommend_report.py)는 `analysis/recommend.py` 의 `compute_composite` / `select_from_universe` / `confidence_label`(=_confidence) / `_company_key` 를 1:1 미러링한다. 한쪽을 고치면 다른 쪽 + `tests/test_rank_parity.py` 도 함께 고친다.
6. **커밋/푸시는 사용자가 요청할 때만.** `master` push 가 곧 배포 트리거다.

---

## 1. 아키텍처 (전부 무료)

- **GitHub Actions** 가 `recommend` 분석 빌드 → `mobile_app/` export → Cloudflare Pages 배포.
  - 스케줄: **하루 2회 KST 07:00 & 09:00** (cron `0 0,22 * * *` = UTC 22:00·00:00) — 미국 장 마감(EDT 05:00 / EST 06:00 KST) **직후**라 그날 일봉 종가를 확실히 잡는다. (이전 4회는 전부 장 마감~개장 사이라 가격이 중복이었음.)
  - push(트리거 경로: `stock_ai/**`, `functions/**`, `tests/**`, `export_mobile_app.py`, 워크플로, `manifest.webmanifest`, `sw.js`, `requirements.txt`) + `workflow_dispatch` 도 트리거.
  - `data_cache/` 를 **주간 키로 actions/cache** (10GB 한도 내) → SEC/yfinance 재수집 최소화.
  - 배포: 임시 `wrangler.toml`(D1 binding `DB`) 생성 후 `npx wrangler@4 pages deploy`. `compatibility_date` 는 과거 날짜로 고정.
- **Cloudflare Pages Functions** `functions/api/[[path]].js`: `/api/health`, `/api/sync-code`, `/api/holdings/{code}`, `/api/quotes`.
- **D1** `sp-profit-plan-holdings`: 테이블 `holdings`(sync-code→JSON), `quote_cache`(symbol→payload). 마이그레이션은 `wrangler pages deploy` 가 자동 적용하지 않음 → 신규 시 수동.
- **가격**: 빌드 시점 = yfinance 일봉 종가(카드·기준가). 장중 실시간 = `/api/quotes`(Yahoo v8 chart, D1 5분 캐시, 앱이 5분마다 폴링). 구 v7 quote 엔드포인트는 Unauthorized라 제거됨, 응답은 `source:"yahoo-chart"`. `/api/quotes` 심볼 캡 20 + chart fetch 8개 청크(무료 50 subrequest 한도 대비).
- GitHub secrets: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_D1_DATABASE_ID`. 변수 `CF_PAGES_PROJECT_NAME=sweetproduct`. `cloudflare-connection.json`(gitignore)에 D1 id.

---

## 2. 현재 리포트(앱) 모습

**데스크탑**: 세로 스택(히어로 → 추천 분석 표 → 종목별 상세 카드 → 내 보유).
**모바일(≤720px)**: 헤더(컴팩트) + **하단 플로팅 아이콘 네비**(추천 분석/종목 상세/내 보유) + **가로 스와이프 페이저**.

- **히어로**: eyebrow(`ranked analysis` 등은 섹션용), 2줄 제목 + 오른쪽 **↻ 갱신 + ⓘ**. 모바일은 eyebrow/lead 숨김, 제목 옆 갱신, 투자성향+알약 한 줄.
  - **↻ 갱신 ⓘ 안내**(데스크탑 hover 툴팁 / 모바일 ⓘ 탭): `순위 갱신 · 미국 장 마감 후 매일 07·09시(KST)` + `시장 오픈 · 매일 22:30~05:00(KST)`. 시장오픈 시간은 빌드시 미국 DST 판별해 자동(여름 22:30~05:00 / 겨울 23:30~06:00).
- **가중치 조절 패널**(접이식): 5축 슬라이더 + **▲▼ 스테퍼(±1)** + **합계 NN/100 게이트** — 합이 정확히 100일 때만 결과 표시. 프리셋 3종(Balance/Risk Taker/Conservative)은 합 100. 슬라이더 `touch-action:pan-y`(세로 스크롤 유지). 선택은 `localStorage('sp_weights')`.
- **추천 종목 분석 표** + **종목별 상세 카드**: 5축 **레이더 차트**(인라인 SVG) + **조건주문 기준가**(매수 고려가=−변동성%눌림 / 손절=−20% / 추세이탈=200일선) + 왜추천·리스크. 표·카드는 `_RANK_JS` 가 `window.SCORE_UNIVERSE` 로 클라이언트 렌더(슬라이더 조작 시 재랭킹).
- **내 보유**: 동기화 코드(D1) + 수량/평균가 + `/api/quotes` 평가손익. 가격 시각은 **기기 로컬(KST)** 표시(asOfEpoch).

---

## 3. 점수 엔진 (5축 멀티팩터)

축: value / quality / growth / trend / sentiment. 서브점수는 **섹터 상대 백분위**(소표본 섹터는 전체 폴백), composite = Σ(축×가중치)/Σ(가중치). 선정 = composite 정렬 + 섹터 최대 3 + 이중상장 dedup.

- **value**: pe, pb, ps, ev_ebitda, fcf_yield(↑), div_yield(↑)
- **quality**: roe, roa, net/gross/op margin, current_ratio, interest_coverage, **piotroski(0~9)**, debt_ratio
- **growth**: revenue/eps growth(YoY) + revenue/eps **CAGR(다년)**
- **trend(위험조정)**: above_200d, momentum 12/6/3m, dist_52w_high, **volatility/max_drawdown/beta(↓ 우대)**
- **sentiment**: yfinance 헤드라인 **lexicon** 키워드 점수(현재 활성, 전 종목). FinBERT 옵션은 저사양/CI 무거움으로 미사용.
- 지표 가중(`_METRIC_WEIGHTS`): piotroski 2×, roe·fcf_yield·momentum_12m 1.5×.

**데이터 출처/주의**:
- SEC `companyfacts`(종목당 1회 캐시, User-Agent만). 신규 팩터는 **같은 캐시 JSON 재파싱**이라 추가 호출 0.
- **태그 전환 주의**: `_annual_points` 는 후보 태그 중 *가장 최신 데이터* 를 가진 개념을 고른다(첫 매칭 X). NVDA 매출이 옛 값으로 잡히던 버그를 이렇게 고침.
- **TTM 금지**: SEC XBRL은 Q4 미분리·회계연도 제각각이라 TTM 합산이 일부 종목 값을 오염(NVDA 등)시킴 → **최신 연간(10-K)** 사용. 굳이 하려면 연간+YTD−전년YTD + sanity 가드.
- beta 용 SPY 가격은 빌드당 1회만 받아 재사용.

---

## 4. 빌드 / 검증 / 배포

```powershell
cd "C:\Users\jin\Desktop\주식관련AI"
python -m stock_ai recommend --top 10 --limit 0      # 전체 S&P500, 감성 ON(lexicon). 캐시 워밍 시 ~15분
python export_mobile_app.py                           # mobile_app/ + zip
pytest -q                                             # 19개 통과 기준
```
- **UI만 바꿀 때 빠른 검증**(5분 재분석 회피): 합성 `FactorRaw`→`compute_scores`→`build_recommendations`+`build_score_universe`→`build_recommend_report(out_path=임시)` 로 렌더해 grep/브라우저 확인.
- **브라우저 라이브 점검**: `python -m http.server` 후 Chrome MCP. ⚠️ 자동화 창은 `innerWidth=0` 으로 보고돼 절대 px가 무의미 → 상대값/`localStorage` 후 reload 로 검증. rAF 의존 코드(슬라이더 render)는 헤드리스에서 안 돌 수 있음.
- **배포**: 검증 후 커밋 → `git push origin master` → Actions 자동 빌드·배포(감성 포함 ~17분 + 큐 가끔 10~15분). 완료 확인: API로 run 폴링(`gh` 없음 → GitHub REST API) → 라이브 `/api/health` `{ok:true}`, `/?cb=...` 마커 grep.
- `node` 로컬 부재 → Worker JS 는 wrangler 가 배포 시 검증.

---

## 5. 서브에이전트 팀 (`.claude/agents/`)

다음 세션부터 이름으로 호출 가능(이번 세션엔 로드 안 돼 general-purpose 로 대행했음).
- **cost-guardian**(읽기전용): $0 가드레일 리뷰
- **quant-data-engineer**: SEC/가격/팩터/스코어링 엔진
- **pwa-report-builder**: recommend_report.py 생성기(디자인·모바일·SW·JS)
- **cloudflare-deploy**: Functions API·D1·Actions 배포
- **release-verifier**(읽기전용): pytest·빌드·라이브 검증

권장 흐름: 변경 → release-verifier 검증 → cost-guardian $0 확인 → (UI는 pwa, 엔진은 quant) → cloudflare-deploy 배포.

---

## 6. 핵심 파일

- `stock_ai/report/recommend_report.py` — 디자인·CSS·`_RANK_JS`(재랭킹/게이트/레이더/기준가)·`_PAGER_JS`(탭/ⓘ)·`_HOLDINGS_JS`·툴팁/시장시간 (가장 중요)
- `stock_ai/analysis/score.py` — 섹터 상대 백분위 + 축 가중 평균, `_AXIS_METRICS`, `_METRIC_WEIGHTS`
- `stock_ai/analysis/factors.py` — `FactorRaw`, `compute_trend`, `compute_risk`, 가격기반 밸류
- `stock_ai/analysis/recommend.py` — `compute_composite`/`select_from_universe`/`build_score_universe`/`_order_levels`(기준가)/`confidence_label`
- `stock_ai/data/fundamentals.py` — SEC 파싱(확장 개념·Piotroski·CAGR), `_annual_points`(최신 태그 선택)
- `stock_ai/runner.py` — 오케스트레이션(SPY 벤치마크 전달, score_universe 생성)
- `functions/api/[[path]].js` — Functions API(quote 캐시·chart fallback·asOfEpoch)
- `.github/workflows/cloudflare-pages.yml` — 빌드/배포(cron 07·09 KST)
- `tests/test_rank_parity.py` — 서버==클라 파리티 고정
- `export_mobile_app.py`, `sw.js`, `manifest.webmanifest`

---

## 7. 남은 일 / 아이디어 (전부 선택, 비용 표기 확인)

- (선택) 카드 "현재가"도 `/api/quotes` 로 5분 라이브 표시(기준가는 빌드 기준 유지). $0.
- (선택) 규칙 기반 트레일링 익절가(고점 대비 −X%) — 예측 아님 톤 유지.
- (선택) 보유 패널에 내 평균단가 기준 손절가 표시.
- (선택) sentiment FinBERT 업그레이드 = CI 빌드시간↑(승인 필요). 현재 lexicon 은 약한 신호.
- (⚠️ 승인 필요) 유니버스 확장(S&P400/600 등) = SEC 호출·빌드시간↑.
- (정리) README/AGENTS/MOBILE_DEPLOY 의 옛 4회 스케줄·`--no-sentiment` 언급 갱신.
- 큐 대기(공개 repo Actions)가 가끔 10~15분 — 정상.

---

## 8. 인수인계 프롬프트 (다음 세션에 붙여넣기용)

아래 블록을 새 세션 첫 메시지로 사용:

```
너는 이 프로젝트를 이어받는 Claude Code다.

프로젝트: C:\Users\jin\Desktop\주식관련AI
GitHub: https://github.com/jinnn99/sp-profit-plan (master)
운영 배포: https://sweetproduct.pages.dev  (현재 HEAD f077487)

먼저 반드시 읽어라(이 순서):
1. HANDOFF_2026-06-10.md  (현재 최신 인수인계)
2. CLAUDE.md              (운영 규칙·불변식)
3. README.md / MOBILE_DEPLOY.md / AGENTS.md

절대 규칙:
- 월 플랫폼 비용 목표 $0. 유료 API·free tier 초과·always-on·크론/트래픽/스토리지 증가 등
  비용 위험 변경은 구현·권장 전에 먼저 경고하고 명시 승인받아라. 불확실하면 멈추고 질문.
- 리포트는 stock_ai/report/recommend_report.py(생성기) 단일 소스. S&P_수익&플랜.html 직접수정 금지.
- 제거된 UI(면책/푸터/메타줄/표안내문/포트폴리오 비중) 복구 금지.
- D1 binding 이름 DB 유지, 서비스워커 /api/ 캐시 금지.
- 순위 파리티: 브라우저 _RANK_JS 가 analysis/recommend.py(compute_composite/select_from_universe/
  confidence_label/_company_key)를 1:1 미러링한다. 한쪽 고치면 다른쪽 + tests/test_rank_parity.py 도.
- 커밋/푸시는 내가 요청할 때만(= master push 가 배포 트리거).

작업 방식:
- 데이터/팩터/스코어링은 quant-data-engineer, UI/리포트는 pwa-report-builder,
  API/배포는 cloudflare-deploy, 검증은 release-verifier, 비용은 cost-guardian
  (.claude/agents/ 에 정의됨) 를 활용해라.
- 변경 후 pytest -q(19 통과 기준) + 실데이터/합성 검증. 배포 전 release-verifier + cost-guardian 통과.
- 배포는 push → GitHub Actions(하루 2회 KST 07·09시 자동, push/dispatch 도). 완료는 GitHub REST API 로
  run 폴링 후 라이브 /api/health 와 마커 grep 으로 확인.

현재 상태 요약: 5축 멀티팩터(가치·품질·성장·추세·심리) 스크리너 PWA. 가중치 슬라이더 재랭킹(합 100 게이트)+
5축 레이더+종목별 조건주문 기준가+모바일 가로 스와이프 페이저+하단 아이콘 네비. 데이터는 SEC EDGAR+yfinance(무료)
재파싱으로 Piotroski·FCF·EV/EBITDA·CAGR·섹터상대평가·위험조정 추세까지 확장. 감성은 lexicon 뉴스로 활성.
전부 $0 유지.

먼저 위 문서를 읽고, 오늘 무엇을 할지 물어라. (요청 없이 임의 변경·배포 금지.)
```
